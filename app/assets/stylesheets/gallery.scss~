@import "bootstrap-sprockets";
@import "bootstrap";

body {
    padding: 60px 10px 10px;
}

.illust-box {
    width: 320px;
    height: 320px;
    padding: 10px ;
    list-style: none; 
}

.illust-box-image {
    max-width: 240px;
    max-height: 240px;
}